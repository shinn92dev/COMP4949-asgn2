import HeroContainer from "../components/container/HeroContainer";

const Home = () => {
    return (
        <div>
            <HeroContainer />
        </div>
    );
};
export default Home;
